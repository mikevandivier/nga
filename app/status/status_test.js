'use strict';

describe('nga.status module', function() {

  beforeEach(module('nga.status'));

  describe('status controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ThisCtrl = $controller('StatusCtrl');
      expect(ThisCtrl).toBeDefined();
    }));

  });
});