'use strict';

describe('nga.home module', function() {

  beforeEach(module('nga.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeControl = $controller('HomeController');
      expect(homeControl).toBeDefined();
    }));

  });
});