'use strict';

describe('nga.planning module', function() {

  beforeEach(module('nga.planning'));

  describe('planning controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ThisCtrl = $controller('PlanningCtrl');
      expect(ThisCtrl).toBeDefined();
    }));

  });
});